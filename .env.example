# Opcional: URL pública para OAuth (solo si expones a internet)
# PUBLIC_URL=https://gbrain.tudominio.com

# API key de api.nan.builders
NAN_API_KEY=sk-TU-KEY-AQUI

# Clave maestra del proxy LiteLLM (genera con: openssl rand -hex 16)
LITELLM_MASTER_KEY=sk-local-master-CHANGE-ME